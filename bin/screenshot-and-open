#!/bin/bash

set -e

FILE="$HOME/screenshots/$(date +%s).png"

if [ ! -z "$WAYLAND_DISPLAY" ]; then
  grim -g "$(slurp)" $FILE
  imv -s none $FILE
else
  maim -s $FILE
  feh $FILE --class __floating
fi

